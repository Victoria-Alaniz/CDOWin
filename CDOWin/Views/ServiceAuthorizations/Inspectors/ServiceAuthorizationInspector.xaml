<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CDOWin.Views.ServiceAuthorizations.Inspectors.ServiceAuthorizationInspector"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CDOWin.Views.ServiceAuthorizations.Inspectors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CDOWin.ViewModels"
    xmlns:control="using:CDOWin.Controls"
    d:DataContext="{d:DesignInstance Type=viewmodels:ServiceAuthorizationsViewModel}"
    mc:Ignorable="d">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" Margin="16,8,8,8"
                   FontSize="22" FontWeight="Bold"
                   TextWrapping="WrapWholeWords"
                   Text="{Binding Selected.client.name}"/>

        <!-- Edit Button -->
        <Button Grid.Row="0" Grid.Column="1" Margin="16,8,12,0" Width="32" Height="32"
                    Padding="0" VerticalAlignment="Top" Style="{StaticResource SubtleButtonStyle}">
            <FontIcon Glyph="&#xE70F;" FontSize="16" Foreground="{ThemeResource SystemAccentColor}"/>
        </Button>

        <!-- Details Caption -->
        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="16,8,8,8" Style="{StaticResource CaptionTextBlockStyle}" Text="Details"/>

        <!-- Detials Grid -->
        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" 
              Margin="12,0,12,8" RowSpacing="12" Padding="12,8,16,12"
              Style="{StaticResource BasicCardContainerGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Row 0: PO Number + Case ID-->
            <control:LabelPair Grid.Row="0" Grid.Column="0" Label="SA ID" Value="{Binding Selected.id}"/>
            <control:LabelPair Grid.Row="0" Grid.Column="1" Label="Case ID" Value="{Binding Selected.clientID}"/>

            <!-- Row 1: Counselor -->
            <control:LabelPair Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Counselor" Value="{Binding Selected.client.counselorReference.name}"/>

            <!-- Row 2: Start Date + End Date -->
            <control:LabelPair Grid.Row="2" Grid.Column="0" Label="Start Date" Value="{Binding Selected.formattedStartDate}"/>
            <control:LabelPair Grid.Row="2" Grid.Column="1" Label="End Date" Value="{Binding Selected.formattedEndDate}"/>

            <!-- Row 3: Unit Cost -->
            <control:LabelPair Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Unit Cost" Value="{Binding Selected.formattedCost}"/>
            <!-- Row 4: Description -->
            <control:LabelPair Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                               Label="Description" Value="{Binding Selected.description}"/>
        </Grid>
    </Grid>
</Page>
